# INSTALLING   
Because of how node-red interacts with nix (not really at all), node-red must be installed with brew, then modules installed via npm.  
OLA is managed via Nix, since it requires a custom build to work with cheap DMX adapters. 

[Install](./doc/INSTALL.MD)
